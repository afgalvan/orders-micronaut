apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jdbc-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jdbc-app
                port:
                  number: 9000
