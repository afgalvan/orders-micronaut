apiVersion: apps/v1
kind: Deployment
metadata:
  name: "Productos"
spec:
  selector:
    matchLabels:
      app: "Productos"
  template:
    metadata:
      labels:
        app: "Productos"
    spec:
      containers:
        - name: "Productos"
          image: "gcr.io/Productos/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "Productos"
spec:
  selector:
    app: "Productos"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080