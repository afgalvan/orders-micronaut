apiVersion: apps/v1
kind: Deployment
metadata:
  name: "Pedidos"
spec:
  selector:
    matchLabels:
      app: "Pedidos"
  template:
    metadata:
      labels:
        app: "Pedidos"
    spec:
      containers:
        - name: "Pedidos"
          image: "gcr.io/Pedidos/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "Pedidos"
spec:
  selector:
    app: "Pedidos"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080